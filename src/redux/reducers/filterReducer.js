const initState = {
  cardName: '',
  setName: '',
  spread: null,
  gainLoss: null,
  dateTo: null,
  dateFrom: null,
};

const filterReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
