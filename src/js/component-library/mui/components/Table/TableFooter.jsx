import React from 'react';
import TableFooter from '@material-ui/core/TableFooter';

const MuiTableFooter = (props) => {
  return <TableFooter>{props.children}</TableFooter>;
};

export default MuiTableFooter;
