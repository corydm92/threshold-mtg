import React from 'react';
import TableSortLabel from '@material-ui/core/TableSortLabel';

const MuiTableSortLabel = (props) => {
  return <TableSortLabel>{props.children}</TableSortLabel>;
};

export default MuiTableSortLabel;
